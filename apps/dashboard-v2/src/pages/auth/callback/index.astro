---
// BetterAuth handles the OAuth callback server-side
// This page is just a placeholder - the actual callback is handled by the API route
// The user will be redirected to the appropriate page after authentication
return new Response(null, {
  status: 302,
  headers: {
    Location: '/overview',
  },
});
---
