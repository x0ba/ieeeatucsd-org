---
import createDashboardPage from '../../components/dashboard/shared/wrappers/createDashboardPage.tsx';
import DashboardHead from '../../components/dashboard/shared/DashboardHead.astro';
import SettingsContent from '../../components/dashboard/pages/settings/SettingsContent.tsx';
import NAVIGATION_PATHS from '../../components/dashboard/shared/types/navigation.ts';

const title = 'Settings - Dashboard';
const currentPath = NAVIGATION_PATHS.SETTINGS;
const SettingsPage = createDashboardPage(SettingsContent, currentPath);
---

<!doctype html>
<html lang="en">
  <head>
    <DashboardHead title={title} />
  </head>
  <body>
    <SettingsPage client:only="react" />
  </body>
</html>
